Inventory synthesized from every JSON under `site-audit-auth/spa-pages` (20), `deep-pages` (56), and `tab-states` (2). Captures are RTL Kurdish UI (`lang: en`, `dir: rtl`), commonly show offline/loading/empty states; no live-site access or edits performed. Dynamic IDs in deep routes are templates, not exhaustive entity instances.

SHARED UI (present across authenticated SPA/deep captures; e.g. `spa-pages/items.json:34-119,121-453`, `deep-pages/addpurchase-9GKQp-Vq429.json:37-117,119-595`): persistent sidebar/breadcrumb links Home `/home`, Dashboard `/Dashboard`, Workplaces `/Workplaces`, User management `/usermanagement`, Account info `/accountinfo`, General configurations `/generalconfigurations`, Accounting `/accounting`, Storehouse `/storehouse`, Purchases `/purchases`, Sales `/sales`, Inventory comparison `/comparestore`, Reports `/reports`, Deleted information `/deletedlogs`; header/sidebar collapse, previous/next page, currency selector, profile menu; favorites add/remove, fullscreen, dark mode, logout; global search, autocomplete toggle, export/download, close/save/new controls. Common login overlay includes username/admin context, password field, Sign in. Repeated “internet unavailable”, Loading, no-data messages. Export, pagination, column-sort/filter controls recur on list tables. Currency/accounting UI supports multiple currencies (labels only; values omitted).

AUTH / LOCALIZATION: `deep-pages/CustomerLogin.json` route `/CustomerLogin` is customer login; credential concepts username/account identifier + password and submit. Auth overlay recurs on all pages. UI is Kurdish-localized, RTL, with English document language metadata. `spa-pages/accountinfo.json` `/accountinfo`: account/profile information; edit/save concepts (account identity/contact/subscription concepts). Customer portal login is the only explicit portal capture; no portal dashboard content captured.

DASHBOARD: `spa-pages/home.json` `/home`, `spa-pages/dashboard.json` `/dashboard`, `deep-pages/account-*.json` (sampled account/detail variants), `deep-pages/boxtransactionreport.json` `/boxtransactionreport`. Home/CRM landing exposes navigation and quick modules. Dashboard has KPI/chart/summary concepts for sales, purchases, inventory, income/expense, debt and account performance; box transaction report is cash-box movements with date/range/account/store filters and debit/credit/balance-style columns. Many dashboard/deep captures were empty/loading.

USERS / ROLES / WORKPLACES / EMPLOYEES: SPA routes `/users`, `/usermanagement`, `/workplaces`, `/employeelist`; deep `/user/<workplaceId>`, `/workplace/<id>`, `/addemployee/<workplaceId>`, plus `groups`, `drivers`. List schemas: user name/login/status, role/group, workplace, employee/contact/job fields, driver assignment/status, group name/members/permissions. Forms: user username/display name, password/password confirmation, role/group, workplace, active flag; employee name/contact/address, job/department, workplace, dates and compensation/accounting concepts; workplace name/code/address/contact/currency and settings; group name + permission matrix; driver identity/contact/vehicle/status. Actions: add/create, edit, delete/disable, search/filter, assign workplace/role, save/cancel, export; employee detail/edit and driver/group management.

CUSTOMERS / ACCOUNTS: SPA `/accounting`, `/accountinfo`; deep `/accounts`, `/account/<id>`, `/accountconfiguration`, `/accountolddebitpurchase`, `/accountolddebitsell`. Account tables/forms cover account/customer name, type (customer/supplier/other), phone/contact/address, balance/debt/credit status, currency, notes, opening/old debit; account detail shows transaction history, invoices, payments and balances. Actions create/edit, open detail, record payment/debt, configure account, export/filter. Old purchase/sell debt pages provide opening-debt entry by account and transaction type. Actual entity names/contact/numbers/values intentionally omitted.

INVENTORY: SPA `/storehouse`, `/items`, `/comparestore`; deep `/item/<workplaceId>`, `/serviceitem/<workplaceId>`, `/services`, `/groups`, `/transferitemlist`, `/transferitem/<workplaceId>`, `/storeregistrylist`, `/storeregister/<workplaceId>`. Product list schema explicitly captured in `spa-pages/items.json:490-510`: image, brand/series, product type, details, barcode, code, purchase price, retail/wholesale/special/extra prices, product display/status. Forms: item/service name, brand/category/details, barcode/code, unit, warehouse, pricing tiers, opening quantity, tax/discount/status; service item lacks stock quantity. Warehouse transfer fields: from/to warehouse, item, quantity, note/date; stock registry fields: warehouse/item movement, quantity, date/type. Actions add/quick-add, edit/delete, favorite, search/filter/sort, export, transfer, stock adjustments, show/hide columns. `item-L9Gzm`, etc. are sampled ID templates.

PURCHASING: SPA `/purchases`; deep `/purchaseinvoices`, `/purchaseorders`, `/addpurchaseorder/<workplaceId>`, `/addpurchase/<workplaceId>/<modeId>`, `/addpurchasepayment/<workplaceId>`, `/accountolddebitpurchase`, `/purchaseissueconfig`. Purchase menu includes invoice, order, payment and old-debt flows. Invoice/order tables: number/date, supplier/account, warehouse, item lines, quantity, unit price, discount, tax/total, payment/debt/status. Add/edit form evidence `deep-pages/addpurchase-9GKQp-Vq429.json:597-684`: warehouse, barcode/item selectors; line columns warehouse, type/details, barcode, quantity, pre-discount purchase price, discount, post-discount price, total, record time; header account/recipient, transaction type (cash/credit), currency rate, warehouse note; summary discount, paid in currencies, total and current/old/total debt. Actions new/edit, add line/search, upload document, save note, delete invoice, payment, close, export. IDs are sampled route parameters.

SALES: SPA `/sales`; deep `/salelist`, `/saletalaflist`, `/selloffer`, `/addsale/<workplaceId>/<saleMode>`, `/addselloffer/<workplaceId>`, `/sellinvoiceclusting/<workplaceId>`, `/accountolddebitsell`. Sale modes visibly include normal sale, credit invoice, return invoice, “talaf”/loss-style invoice, payment collection, and sales offer (`spa-pages/sales.json:107-154`). Schemas parallel purchasing: invoice/customer, warehouse, item/barcode, quantity, prices, discount, tax, payment method/currency, due/old debt, dates/status; offers add validity/notes. Actions create/edit/delete, return, collect payment, upload, print/export, search/filter, favorite.

INCOME / EXPENSE / DEBT: SPA `/income`, `/expense`, `/accounting`, `/AddDebt`; deep `/deletedincomes`, `/deletedexpenses`, old purchase/sell debt routes. Forms conceptually date, account/category, description/note, amount/currency, payment method/cashbox, attachment, employee/workplace; debt form adds debtor/creditor, due date, opening/current debt and settlement/payment. Lists include date, account, category, description, currency/amount, status and actions; create/edit/delete, settle, export/filter.

PROFIT / LOSS / CAPITAL: SPA `/profitandlossdashboard`; deep `/profitsAndLosses`, `/profitAndLossesbalance`, `/addProfitAndLoss/<workplaceId>`, `/addProfitAndLossbalance/<workplaceId>`. Profit/loss list/report concepts: period/date, account/category, revenue, cost/expense, profit/loss, currency, notes; balance/capital concepts: opening/closing capital, assets/liabilities/equity and adjustment entries. Forms add period, account/category, debit/credit or amount, description and date; actions add/edit/delete, save, calculate/view dashboard, export. Route IDs sampled.

REPORTS: SPA `/reports`, `/comparestore`; deep `/reportaccounts`, `/reportcheckinventory`, `/reportstockbalancesheet`, `/boxtransactionreport`, `/sellinvoiceclusting/<workplaceId>`. Report selector covers account statement, sales/purchase, income/expense, debt, stock check/count, stock balance sheet, warehouse comparison, cash-box transactions, invoice clustering/collection. Shared filters: date range, workplace/warehouse, account/customer, item/category, transaction/status, currency; outputs tables/charts with totals and export/print. Stock schemas include item/code, warehouse, opening/in/out/closing quantities and variance; account report includes date/reference/type/debit/credit/balance. `reportaccounts.json`, `reportcheckinventory.json`, `reportstockbalancesheet.json` provide deep samples.

DELETED RECORDS: SPA `/deletedlogs`; deep `/deletedexpenses`, `/deletedincomes`, `/deletedpurchaseinvoices`, `/deletedpurchaseitems`, `/deletedsaleinvoices`, `/deletedsaleitems`. Separate tables for deleted expense/income, purchase invoice/item, sale invoice/item; schemas retain record type, date/reference, account/item, quantities/amount concepts, deleted-by/time and reason/status. Visible actions are search/filter, inspect, restore/permanent-delete where available, export; captures are often empty.

SETTINGS: SPA `/generalconfigurations`, `/accounting`; deep `/Storeconfig`, `/accountingconfigurations`, `/purchaseissueconfig`, `/accountconfiguration`. `spa-pages/generalconfigurations.json:378-416` explicitly captures tabs: quotas/share ratios (table name + percentage), currency, capital, system options, printing. Controls include save/new, increment/decrement and close. Settings concepts: currency definitions/rates/decimal/display; capital/opening balances; quota/commission percentages; system/autocomplete/numbering/tax/rounding; print templates/rich text. Store config covers store/workplace defaults, invoice/receipt and stock behavior. Purchase issue config covers purchase numbering/validation/options. Accounting config covers account categories, payment/cashbox and journal behavior.

CUSTOMER PORTAL: `deep-pages/CustomerLogin.json` only. Login route and credential fields are captured; subsequent portal landing/account/order/invoice content was not captured in the inventories, so no unsupported portal features are asserted.

TAB STATES / SERVER INSTABILITY: `tab-states/dashboard.json` route `/dashboard` and `tab-states/Storeconfig.json` route `/Storeconfig` both report `tabCount: 0`, `states: []`; therefore no tab labels or active tab content were captured there. The general-settings tab labels above came from the SPA page itself, not tab-state replay. Across inventory JSON, offline/loading/empty responses mean schemas/actions are based on captured controls, headings, links, table headers, and route names—not claims that every backend record rendered.