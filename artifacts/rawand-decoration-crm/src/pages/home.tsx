import { NavTile,SectionTitle,TileGrid } from "@/components/crm";
import { HOME_GROUPS } from "@/lib/navigation";
export default function Home(){
 return <div className="home-page"><h1>Rawand Decoration CRM</h1>{HOME_GROUPS.map(group=><section key={group.title} className={group.title==="CRM"?"home-crm":""}><SectionTitle>{group.title}</SectionTitle>{group.title==="CRM"?<><TileGrid className="home-crm-featured">{group.links.slice(0,4).map(link=><NavTile key={link.path} href={link.path} title={link.title} icon={link.icon}/>)}</TileGrid><TileGrid className="home-crm-rest">{group.links.slice(4).map(link=><NavTile key={link.path} href={link.path} title={link.title} icon={link.icon}/>)}</TileGrid></>:<TileGrid>{group.links.map(link=><NavTile key={link.path} href={link.path} title={link.title} icon={link.icon}/>)}</TileGrid>}</section>)}</div>;
}