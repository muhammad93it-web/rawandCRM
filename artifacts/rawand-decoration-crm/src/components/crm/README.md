# CRM UI kit

List pages compose `Toolbar`, `DataTable`, and `FilterDrawer`; pass table columns with stable keys and
render functions, and put page-specific filter fields inside the drawer. Form pages compose `FormGrid`
with the field components and finish with `AccentButton`. All components inherit the application RTL
direction and the active zoom/theme tokens.